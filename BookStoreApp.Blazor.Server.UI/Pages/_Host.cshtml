@page "/"
@namespace BookStoreApp.Blazor.Server.UI.Pages
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
	Layout = "_Layout";
}

@*
    If you're trying to use localStorage or other JS feature, you can't set pre-rendered.
    otherwise set it to "Server" rather than "ServerPrerendered"
*@
<component type="typeof(App)" render-mode="Server" />
